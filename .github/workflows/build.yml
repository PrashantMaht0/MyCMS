name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Resolve package dependencies
        run: |
          xcodebuild \
            -project MyCMS.xcodeproj \
            -resolvePackageDependencies

      - name: Build and test
        run: |
          xcodebuild \
            -project MyCMS.xcodeproj \
            -scheme MyCMS \
            -configuration Debug \
            -destination 'platform=macOS' \
            CODE_SIGNING_ALLOWED=NO \
            test
